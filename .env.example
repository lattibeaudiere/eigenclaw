# Copy this to .env and fill in your values.
# NEVER commit .env to git — it's in .gitignore.
#
# At deploy time, pass secrets via:
#   ecloud compute app deploy --env CHUTES_API_KEY=xxx --env CHUTES_ENDPOINT=xxx

# ════════════════════════════════════════════════════════════════════════════════
# CRITICAL — Chutes (OpenClaw's LLM brain)
# Without these, OpenClaw has no model to reason with and won't function.
# ════════════════════════════════════════════════════════════════════════════════

# 1. Sign up at https://chutes.ai
# 2. Dashboard → API Keys → copy your key
CHUTES_API_KEY=your_chutes_api_key_here

# Your Chutes model endpoint — two options:
#
# Option A: Use a pre-deployed public model from the Chutes marketplace
#   Browse at https://chutes.ai/app  and find an OpenAI-compatible model
#   Endpoint format: https://<username>--<chute-name>.chutes.ai
#
# Option B: Deploy your own (from chutes/inference_chute.py):
#   chutes deploy chutes/inference_chute.py:chute
#   Then use the URL it gives you
CHUTES_ENDPOINT=https://your-chute-slug.chutes.ai

# Model name your chute exposes (must match what's deployed)
CHUTES_MODEL=Llama-3.2-11B-Vision-Instruct

# ════════════════════════════════════════════════════════════════════════════════
# EigenAI (fallback — used if Chutes is not configured)
# ════════════════════════════════════════════════════════════════════════════════
# Get from: ecloud auth generate --store (already done)
# or from: https://developers.eigencloud.xyz
EIGENCLOUD_API_KEY=your_eigencloud_api_key_here

EIGENAI_BASE_URL=https://eigenai-sepolia.eigencloud.xyz/v1
EIGENAI_MODEL=gpt-oss-120b-f16

# ════════════════════════════════════════════════════════════════════════════════
# EigenCompute TEE (managed automatically — read-only reference)
# ════════════════════════════════════════════════════════════════════════════════

# Visible to users for transparency (the _PUBLIC suffix is OpenClaw convention)
NETWORK_PUBLIC=sepolia

# TEE Mnemonic — auto-injected by EigenCompute KMS at runtime.
# NEVER set this manually for production. Local dev placeholder only.
# NEVER log it. Only derive the wallet address from it.
MNEMONIC=

# ════════════════════════════════════════════════════════════════════════════════
# DeFEyes (Enrichment + auditing target)
# ════════════════════════════════════════════════════════════════════════════════

# Known-good base URL (current product endpoint)
DEFEYES_BASE_URL=https://defeyes-api.vercel.app

# Internal Pro key (pass via EigenCloud KMS / deploy-time env)
DEFEYES_API_KEY=

# ════════════════════════════════════════════════════════════════════════════════
# Arbitrum JSON-RPC (On-chain ground truth)
# ════════════════════════════════════════════════════════════════════════════════
#
# Defaults to https://arb1.arbitrum.io/rpc if unset.
ARBITRUM_RPC_URL=

# OpenClaw dashboard auth token (used by Control UI)
OPENCLAW_TOKEN=

# ════════════════════════════════════════════════════════════════════════════════
# DeFive automation pulse (runs inside the container, no host cron needed)
# ════════════════════════════════════════════════════════════════════════════════

# Enable/disable the 4-hour headless audit pulse
ENABLE_AUDIT_PULSE=true

# Interval in seconds (default: 14400 = 4 hours)
AUDIT_PULSE_SECONDS=14400

# How much to sample from the explorer feed per run
AUDIT_EXPLORER_LIMIT=10
AUDIT_MAX_TXS=5

